// packages
import React from "react";

// css
import "../style/navbar.scss";

const NavBar = props => {
  return (
    <nav id="navBar">
      <h1 id="brand">Brewery App</h1>
    </nav>
  )
}

export default NavBar;
